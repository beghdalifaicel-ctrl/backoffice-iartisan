import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Agents IA — iArtisan Back-Office',
};

export default function AgentsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
